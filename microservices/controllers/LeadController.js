const models = require('../models')
const Lead = models.Lead
const { Op } = require("sequelize")

const index = async data => {
    let where
    if (data.name) {
        
    }
}

module.exports = {
    index
}
